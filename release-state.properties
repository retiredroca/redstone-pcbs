# Platform ids written back by the release workflow. Kept separate from
# versions.properties so a version bump never clobbers them.
#
# Keys are scoped to the Minecraft version (suffix .<mc>), so each supported version keeps its own
# Modrinth version id.
#
# mr.<component>.version.<mc> : Modrinth version id (the previous version is archived on upload).
#
# This file is committed and updated by CI. Leave it in place even when publishing is disabled.
mr.redstonepcbs.version.1.21.1=
