# Per-module versions. Bump the module you are releasing; the release workflow also updates these.
# The key must match the module's `id` in versions/<mc>/<module>/module.properties.
redstonepcbs=1.0.1.26092517
